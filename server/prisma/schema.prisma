generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model SessionLog {
  id        Int      @id @default(autoincrement())
  name      String
  age       String
  gender    String
  location  String
  createdAt DateTime @default(now())
}

